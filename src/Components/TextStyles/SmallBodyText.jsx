import React from "react";

const SmallBodyText = ({ children }) => {
    return (
        <p className="mt-2 font-lato text-[#FFFFFF] opacity-50 font-medium text-[18px]">
            {children}
        </p>
    );
};

export default SmallBodyText;
