import React from "react";

const MediumText = ({ children }) => {
    return (
        <p className="font-lato text-white text-[30px] font-bold text-center">
            {children}
        </p>
    );
};

export default MediumText;
